#!/bin/bash
# post-merge hook - Sync validation bundle after pull/merge
#
# Auto-generated by scripts/install_hooks.sh
# This hook runs after git pull to ensure validation files are synced.

if [ -x scripts/validation_sync.sh ]; then
  scripts/validation_sync.sh || true
fi

if [ -x scripts/ensure_venv.sh ]; then
  scripts/ensure_venv.sh || true
fi
