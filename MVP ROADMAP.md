Goal : Build a working prototype that can :
    - Parse a simple neural network definition (like your MyModel example).
    - Validate tensor shapes across layers.
    - Generate TensorFlow/PyTorch code.
